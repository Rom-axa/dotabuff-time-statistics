export enum GamesPlayersQty {
    solo = 1,
    party = 2,
    all = 0,
};

export enum GameModes {
    allPick = `All Pick`,
    turbo = `Turbo`,
    singleDraft = `Single Draft`,
    other = `Other`,
}